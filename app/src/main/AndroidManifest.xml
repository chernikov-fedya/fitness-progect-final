<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Registr.MainActivityChoose"></activity>
        <activity android:name=".Man.ProfileActivity" />
        <activity android:name=".Registr.MainActivityDisease" />
        <activity android:name=".Train.Desc" />
        <activity android:name=".Train.MainActivityTrain" />
        <activity android:name=".MainActivityMenu" />
        <activity android:name=".Registr.MainActivityRegistr" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>